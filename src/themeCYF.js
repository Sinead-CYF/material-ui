import { createTheme } from "@mui/material/styles";

const themeCYF = createTheme({
  palette: {
    primary: {
      main: "#d12f2f",
    },
    secondary: {
      main: "#29539b",
    },
  },
});

export default themeCYF;
