import { createTheme } from "@mui/material/styles";

const themeDark = createTheme({
  palette: {
    primary: {
      main: "#29539b",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default themeDark;
