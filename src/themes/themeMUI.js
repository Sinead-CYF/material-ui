import { createTheme } from "@mui/material/styles";

const themeMUI = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default themeMUI;
